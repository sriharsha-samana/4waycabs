/**
 * @author Sriharsha Samana
 * 
 * Server class is to connect exchange package classes with core business code
 * Server class takes the responsibility of receiving arguments in JSON format and 
 * also packs return value in JSON format
 *
 * StaticResourceFilter class is to filter static resources like css, js and images from servlet mapping 
 * in web pages  
 * 
 * SubmitPage class is to redirect submitted pages to home page
 * 
 * UploadFiles class is used to upload client files to server to a specific folder
 */
package connect;